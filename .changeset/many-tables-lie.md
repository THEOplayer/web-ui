---
'@theoplayer/web-ui': patch
---

Add `--theoplayer-padding-bottom` CSS property to override the bottom padding that could get applied due to (1) a set `--theoplayer-aspect-ratio` or (2) presence of the `fluid` attribute in a `<theoplayer-default-ui>`.
