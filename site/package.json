{
  "name": "site",
  "description": "Documentation website for Open Video UI.",
  "homepage": "https://theoplayer.github.io/web-ui/",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "npx @11ty/eleventy --input=. --output=../_site",
    "serve": "npm run build -- --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
